{% extends 'base.html.twig' %}

{% block title %}Bonbons{% endblock %}

{% block body %}
    <h1>Bonbons</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bonbon.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ bonbon.titre }}</td>
            </tr>
            <tr>
                <th>Descrption</th>
                <td>{{ bonbon.descrption }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bonbons_index') }}">back to list</a>

    <a href="{{ path('app_bonbons_edit', {'id': bonbon.id}) }}">edit</a>

    {{ include('bonbons/_delete_form.html.twig') }}
{% endblock %}
